#include "main.h"

/**
 * _abs - computes the absolut valeur
 * @n: the integer to compute
 *
 * Returne: the absolute value of n
 */
int _abs(int n)
{
	
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
