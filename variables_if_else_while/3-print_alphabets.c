#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char mot;

	mot = 'a';
	while (a <= 'z')
	{
		putchar(mot);
		mot++;
	}

	mot = 'A';
	while (A <= 'Z')
	{
		putchar(mot);
		mot++;
	}
	putchar('\n');

	return (0);
}
